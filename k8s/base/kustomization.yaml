apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: car-api

resources:
  - namespace.yaml
  - deployment.yaml
  - service.yaml
  - configmap.yaml
  # secret.yaml is managed manually via kubectl (contains real credentials)
